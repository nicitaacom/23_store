# plan-22 — Storage buckets by purpose, folders by email, filenames by slug

## 0. Why this exists

Images are stored under a folder named after the uploader's `auth.users.id`. `auth.users` is
Supabase-managed and read-only from this app — it is never exported or imported directly. What
gets exported is `public.23_users.csv`, and each row's `id` there is the account's OLD uuid from
project A (Supabase A's own `auth.users`). Importing into a fresh project B, B's `auth.users` has
no row for any of these people yet, so `POST /api/backup/auth-users` creates a brand-new B-side
account with a brand-new uuid for each one. `23_users.id` is then rewritten from the old
project-A uuid to that new project-B uuid before the row is upserted — required by
`23_users.id REFERENCES auth.users(id)`: inserting the old project-A uuid into project B would
fail the foreign key, since no `auth.users` row with that uuid exists there. Every image folder,
though, was written under the old project-A uuid and stays there — nothing in Storage knows a
remap happened. Nikita hit this directly: after restoring, his product images 404'd because their
folder was the old uuid, and the running session now has the new one.

Everything that stores images also mixes unrelated concerns in one bucket today
(`23_public-images` holds products, personalized designs, support-chat images, and AI-search
generated images together) under a name that describes none of them.

> **Priority:** P1
> **Screenshot:** none saved under `TODO/` — three screenshots pasted directly in chat (Storage
> folder browser showing the doubled-id folders and loose root files; the Tables/Files backup
> result cards)
> **Recommended model:** Opus · high thinking
> **Status:** see [plan-00-tracker.md](plan-00-tracker.md)

## 1. Decisions made (do not re-open)

Quoted verbatim, in the order Nikita gave them:

- **The slugified email, not `auth.users.id`, is the identity to key Storage on**: "I think the
  only way to fix this is to solely rely on table 23_users and ignore data that comes when I use
  `supabase.auth.getUser()`... just use user email in bucket and not allow to change email (only
  add additional one)." Precise form: `@` and `.` are removed with nothing in their place, not
  replaced with `-` — `email@example.com` → `emailexamplecom`.
- **"Add additional one" means an additional login method, never an additional email** — one
  account has exactly one email, permanently: "user still has only 1 email so it's no way for
  example to have like login with email1@example.com but add a google with email2@example.com.
  backup login means email+password login OR google login with same email." Linking a provider
  to an existing account only ever succeeds under that account's one email — this is what makes
  `slugifyEmail(email)` safe as a permanent key: not just "email rarely changes," but the schema
  never allows an account to hold a second one.
- **Why `23_users.id` has to track `auth.users.id`** (documents the existing
  `app/utils/publicUserSync.ts` behavior, not new work): "when new user register or continue with
  google it's no row in auth.users table - so supabase creates one - that's why I need to sync my
  public.23_users table with auth.users table that's why I need to update id column with that id
  (uuid) that supabase autogenerated for auth.users."
- **Why email, not just keeping `23_users.id` in sync**: "we could also update id in 23_users
  btw every time we continue with google or login but this would break logic of storage files
  because we still need to use email as folder name." `23_users.id` is allowed to change (on
  purpose — that is what lets a restored or re-authenticated account keep working), a Storage
  folder name is not; email is the one value that never moves under any of that.
- **No anonymous uploads for products or personalized designs**: "this is wrong thinking because
  I not allow anonymous users to upload images... if you mean support chat then yes."
- **Anonymous support images get one shared, sweepable space**: "one shared folder for all guest
  users makes sense because this is simpler to clean up this folder with cron schedule. So create
  cron schedule that would cleanup it (images of anonymous users) every week."
- **The `%` symbol is spelled out, not dropped**: "instead of 'сливки 30%' should be
  'slivki-30pct'... I was wrong about 'p' for % - actually should be 'pct'."
- **Function rename**: "I'd rename formatFileNameForBucket to slugifyFileNameForBucket."
- **Product filenames come from the product title, never the pasted file's own name**: "that's
  the issue with copy paste, cuz when user paste image it's name becomes image.png... when user
  add a new product then it needs to take a product name and slugify it so even if owner paste 10
  different images then it will be 'product-name-1.jpg' 'product-name-2.jpg' etc."
- **Products get a `productId` folder, not a flat email folder**: "actuall filepath is
  `useremailexamplecom/productid/product-image-1.jpg` so it needs to be productid there because
  it's easier to delete folder with images instead of fetching all useremailexamplecom/productid
  to delete or instead of having mess from 1000+ images in useremailexamplecom/productid."
- **Chat images get a timestamp filename**: "when user (any - including anonymous) paste image in
  chat - it's nothing to get image name from that's why it should be a date like
  2026-07-29_at_22-19-54.png."
- **Support images get their own bucket, split further by whether the sender is signed in**:
  "if user authenticated it should be separated bucket for it (don't mix product images and
  support images in same bucket)" — then, asked to confirm scope: "support bucket split for
  everyone including anonymous users but all anonymous users gets their own folder that will be
  cleaned up every week... I'd rather to have separated bucket solely for anonymous users."
- **Correction: one folder per anonymous visitor inside `23_support-guest-images`, not one
  shared folder** — a single flat `guest/` folder was the wrong design: "it should be folder for
  each anonymousId because in that way we can select images for a specific gues only. Also
  re-use deviceId in that way guest visit website one and it's tracked only once." Folder =
  the visitor's `deviceId` (already resolved by the UTM system's identity layers, `useDeviceIdStore`
  in `app/store/user/useDeviceIdStore.ts`, documented in `app/[locale]/(site)/stats/dev_readme-device-id.md`)
  — reusing it here means the same visitor gets the same folder across visits, instead of a new
  identity every time a weaker cookie-only id would produce.
- **`23_public-images` is retired as a name**: "23_public-images is a wrong bucket name and
  should be renamed 100%." Starting set, written directly into `app/ts/types/TBuckets.ts` by
  Nikita, grown to 6 once the two questions below were answered:
  ```ts
  export type TBuckets =
    | "23_support-guest-images"
    | "23_support-images"
    | "23_avatar-images"
    | "23_product-images"
    | "23_ai-product-images"
    | "23_product-personalization-images"
  ```
- **Two build stages**: "PLAN STAGE 1 — update all usage of all buckets everywhere... then PLAN
  STAGE 2 is to fix import export of tables and files."
- **One diagram per workflow**, "deep dark-green background, slightly rounded corners": support
  images, product images, avatar URL, and the backup import/export workflow.
- **Correction: the deliverable is the image prompt, not a generated page** — "I need a prompts
  for images not artifact." The 4 finished prompts are §5; Nikita runs them through an image
  generator himself.

### Resolved — the 3 questions above, answered

- **AI-shopping-assistant images get their own bucket, `23_ai-product-images`** — corrects how
  this plan described the feature: "I think yes but it's not an AI search - this is AI helps
  users to shop and generate images and ask if user would like to add an image into a card." Not
  a search feature; a shopping assistant that generates an image and offers to add it to the
  cart.
- **Correction: these images are per-user too, not folder-less** — "should be per user, from our
  experience use slugified user email like useremailexamplecom in order to create folder name."
  `folder: slugifyEmail(email)`, and **no guest folder at all**: "I think I have logic that
  require to login before chatting with AI so it's no sense from that folder." Confirmed in code —
  `handleSubmit` in `useAIChat.ts` returns early with a toast + the AuthModal for a visitor who is
  not signed in. The ✨ generate-image button in `ChatInput.tsx` was NOT behind that gate, so the
  guest branch was reachable through it; both ways in now share one `isSignedIn()` check.
- **Personalized designs get their own bucket, `23_product-personalization-images`** [spelling as
  given — flag if this is a typo before Stage 1 runs the create-bucket SQL, a bucket id has no
  in-place rename once files exist in it], path `email/productId/filename.ext` — e.g.
  `useremailexamplecom/productid/my-kovrik.jpg`. The filename is the buyer's own uploaded file
  name, slugified — unlike a chat paste, `PersonalizeModal.tsx`'s `<input type="file"
accept="image/*">` (line ~118) preserves the real name the buyer picked, so there is something
  worth slugging here, unlike chat's always-generic `image.png`.
- **No migration script — this is a manual, one-time step Nikita runs himself**: "I would
  one-time manually extract everything from .gzip and then rename my previous id into
  myemailgmailcom and in that way I will import files." Code only ever has to support the
  _current_ folder scheme — building logic to detect and re-map the old scheme is pointless: "no
  need to create some logic that would re-order stuff cuz it's no way to do this because you
  don't know user id of new user created in supbaseB to link images from supabaseA." This
  confirms Stage 2 item 9 (relink reconstructs the expected path from email + title) is required,
  not optional — after Nikita's manual rename, the Tables import's `img_url` strings (old-scheme
  paths) will not match anything in Storage until relink can compute the _new_-scheme path from
  each row's current owner + title and match against that instead.

## 2. Code patterns to follow

Pin `dev_readme-code-patterns.md` (all 15 rules, including the banned-words list) and
`good-bad-examples.md` before writing any code from this plan — validate the diff against both,
line by line, before calling a task done. Also pin the dev_readme of every folder this plan
touches: `dev_readme-backup.md`, `dev_readme-supbase-sql.md`,
`app/functions/dev_readme-create-product.md`,
`app/components/ui/Modals/PersonalizeModal/dev_readme-personalize.md`.

Relevant rules from `dev_readme-code-patterns.md` for this plan specifically: rule 5 (return a
string error, don't throw, unless the file already throws), rule 9 (no `export const` for
`className`s), rule 13 (no one-letter/vague names), rule 14's banned-word list — the same guard
already rewrote several lines while this plan itself was being drafted, so re-check the final
code diff against that list too, not only this document.

| File                                                                                                      | Behavior today                                                                                          | Behavior after this plan                                                                                                                                                                       |
| --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/ts/types/TBuckets.ts`                                                                                | `"23_public-images" \| "23_avatar-images"`                                                              | 6 purpose-named buckets (started by Nikita's edit, grown per §1's resolved questions)                                                                                                          |
| `app/functions/support/image/formatFileNameForBucket.ts`                                                  | Keeps spaces/mixed case, drops non-Latin input, no folder is derived from anything reusable             | Renamed `slugifyFileNameForBucket`; lowercase, transliterate, `%`→`pct`, non-alphanumeric runs → single `-`                                                                                    |
| `app/functions/uploadImageFn.ts`                                                                          | Prefixes `folder` once (fixed in `d1e91c8a`), no timestamp/title-based naming                           | Calls the new slug utility for a title (products) or a timestamp (chat)                                                                                                                        |
| `app/functions/createProductHelpers.ts` (`uploadProductImages`)                                           | `folder: crypto.randomUUID()` (from `8ab4842c`), no title/productId used                                | `folder: \`${slugifyEmail(email)}/${productId}\``, filename `slug(title)-{index}.ext`                                                                                                          |
| `app/functions/createProductFn.tsx`                                                                       | Uploads images, then creates the Stripe product                                                         | Creates the Stripe product first so `productId` exists before the image folder is built                                                                                                        |
| `app/components/ui/Modals/AdminPanel/components/FormatImagesForm.tsx`                                     | Calls `uploadProductImages(files, tGlobal)`, no title passed                                            | Passes the product's own title + `id` (already in scope) through                                                                                                                               |
| `app/components/ui/Modals/UpdateAvatarModal.tsx`                                                          | `folder: user.id`, bucket `23_avatar-images`                                                            | `folder: slugifyEmail(user.email)`, bucket unchanged                                                                                                                                           |
| `app/components/ui/Modals/PersonalizeModal/functions/uploadDesignFn.ts`                                   | `folder: \`personalized/${userId}\``(uuid from`getUserId()`), bucket `23_public-images`                 | bucket `23_product-personalization-images`, `folder: \`${slugifyEmail(email)}/${productId}\``, filename = the buyer's own uploaded file name, slugified (not the `crypto.randomUUID()` suffix) |
| `app/[locale]/(site)/components/AISearch/utils/aiFunctionHandlers.tsx`, `.../AISearch/hooks/useAIChat.ts` | `bucket: "23_public-images"`, no folder                                                                 | `bucket: "23_ai-product-images"`, `folder: slugifyEmail(email)` - that chat needs a sign-in, so no guest folder                                                                                |
| `app/functions/support/uploadImagesAndSendMessage.ts`, `.../MessagesFooter.tsx`                           | No folder, no bucket split — every file lands loose at bucket root                                      | Signed-in: `23_support-images`, `folder: slugifyEmail(email)`. Anonymous: `23_support-guest-images`, `folder: deviceId` from `useDeviceIdStore.getState().storedDeviceId`                      |
| `app/api/products/delete/route.ts`                                                                        | Hardcoded `"storage/v1/object/public/23_public-images/"` prefix and `.storage.from("23_public-images")` | `23_product-images`                                                                                                                                                                            |
| `app/api/backup/backupConfig.ts` (`BACKUP_BUCKETS`)                                                       | 2 buckets                                                                                               | 6 buckets                                                                                                                                                                                      |
| `app/api/backup/relink-storage-urls/route.ts`, `backupStorageRelink.ts`                                   | Relinks only by exact exported `bucket/path` match                                                      | Also reconstructs the expected path from the row's current email + title (Stage 2, designed once Stage 1 lands)                                                                                |
| `dev_readme-supbase-sql.md`                                                                               | 2 bucket create+policy blocks, 1 `pg_cron` job (`cleanup_anonymous_tickets`)                            | 6 bucket blocks, a 2nd `pg_cron` job sweeping `23_support-guest-images`                                                                                                                        |

## 3. Terminology

- **Slug** — a title rewritten to lowercase letters, digits and single `-` separators only, so it
  never needs percent-encoding inside a URL. `"сливки 30%"` → `"slivki-30pct"`.
- **`slugifyEmail`** — an email address for use as a folder name, with `@` and `.` removed
  entirely (not replaced with `-`) and everything lowercased: `nicitaacom@gmail.com` →
  `nicitaacomgmailcom`.
- **`productId`** — the Stripe product id. Used as the per-product Storage folder so one
  product's images live in exactly one deletable folder.
- **Guest bucket** — `23_support-guest-images`, where an anonymous (not signed in)
  support-ticket sender's images go. One folder per `deviceId` inside it (not one shared folder
  for every anonymous visitor), swept weekly.
- **Relink** — the existing backup step (`POST /api/backup/relink-storage-urls`) that rewrites a
  restored row's image URL to the current Supabase project once the exact file is confirmed
  present in Storage.
- **Stage 1 / Stage 2** — Nikita's own split: Stage 1 is only the bucket rename (every `bucket:`
  argument in the codebase points at its correct new bucket, nothing about folder shape changes).
  Stage 2 is the folder/filename rework (email, productId, slugged titles, timestamps) plus
  making backup import/export and relink understand that new shape.

## 4. How it should work

**Storage tree, before:**

```
23_public-images/
├── c6e11b7a-.../c6e11b7a-.../2pcs-of-beard-bib_<uploadBatchId>_1.avif   (old admin id, doubled)
├── 1pc-of-beard-bib-1_price_1TIRAxDEq5VtEmnoKqCekBvA.avif               (loose at bucket root)
├── aloe-vera-1_price_1TIR0tDEq5VtEmno5o5TgVVm.avif                     (loose at bucket root)
└── ...

23_avatar-images/
└── <auth-user-id>/avatar.png
```

**Storage tree, after (both stages complete)** — the guest folder is the deviceId in its transport
form, the string `useDeviceIdStore` holds, not the `23-…` signed id. Decoding one needs
`node:crypto`, which is server-only, and the transport form is 1:1 with the signed id, so the same
visitor still lands in the same folder on every visit:

```
23_product-images/
└── nicitaacomgmailcom/
    └── prod_T1IRAxDEq5VtEmno/                 (productId folder — the one folder to delete)
        ├── slivki-30pct-1.jpg
        └── slivki-30pct-2.jpg

23_avatar-images/
└── nicitaacomgmailcom/
    └── avatar.png

23_support-images/
└── nicitaacomgmailcom/
    └── 2026-07-29_at_22-19-54.png             (signed-in sender)

23_support-guest-images/
└── Q5UUMP4MX0LbwF0Eekm3JIeIBwqWeDX0-/          (deviceId — same visitor, same folder every visit)
    └── 2026-07-29_at_22-20-11.png             (swept weekly if no message still references it)

23_ai-product-images/
└── nicitaacomgmailcom/
    └── generated_image.png                    (AI chat needs a sign-in - no guest folder)

23_product-personalization-images/
└── nicitaacomgmailcom/
    └── prod_T1IRAxDEq5VtEmno/
        └── my-kovrik.jpg                       (buyer's own uploaded file name, slugified)
```

**Data path — why the folder key finally holds still after restoring a Tables backup:**

```
Supabase A: user.id lives in auth.users, exported into 23_users.csv as row.id
        │
        ▼
23_users.csv imported into Supabase B — that row.id is still Supabase A's uuid
        │
        ▼
Supabase B's auth.users has no row under that uuid (this person never signed up here)
        │
        ▼
POST /api/backup/auth-users creates one — a brand-new Supabase B auth.users row, brand-new uuid
        │
        ▼
23_users.id (and every FK: owner_id, sender_id, ...) gets rewritten from the
Supabase A uuid to the new Supabase B uuid, so 23_users.id matches Supabase B's
own auth.users.id — required by 23_users.id REFERENCES auth.users(id)
        │
        ▼
the account's email never changed through any of this
        │
        ▼
slugifyEmail(email) → same Storage folder as before the restore
```

**Build order:**

One commit per numbered step below, not one combined commit per stage and not one commit for the
whole plan. Each commit's own description covers just that step, per `commit-patterns.md` — a
reviewer reading the commit log should see the same steps as this plan, in order.

**Stage 1 — bucket rename only:**

1. Supabase SQL: replace `dev_readme-supbase-sql.md`'s **SQL query for buckets + policies**
   section's `23_public-images`/`23_avatar-images` blocks with all 6 buckets from §1's
   `TBuckets` union, then run it in the Supabase SQL Editor (🚨 manual step, SQL in the commit —
   `23_avatar-images`, `23_support-images`, `23_support-guest-images` and
   `23_product-personalization-images` all upload with `upsert: true`, so each needs its own
   UPDATE policy the same way `23_avatar-images` already does; `23_product-images` and
   `23_ai-product-images` never upsert, so INSERT+SELECT is enough, matching today's
   `23_public-images` block):

   ```sql
   -- =================================== STORAGE BUCKETS ===================================
   -- 23_product-images, 23_ai-product-images: never upsert, INSERT+SELECT only
   -- 23_avatar-images, 23_support-images, 23_support-guest-images, 23_product-personalization-images:
   --   upload with upsert:true, so each also needs its own UPDATE policy
   DO $$
   DECLARE
     bucket_id TEXT;
   BEGIN
     FOREACH bucket_id IN ARRAY ARRAY[
       '23_product-images', '23_ai-product-images', '23_avatar-images',
       '23_support-images', '23_support-guest-images', '23_product-personalization-images'
     ]
     LOOP
       INSERT INTO storage.buckets (id, name, public)
       VALUES (bucket_id, bucket_id, true)
       ON CONFLICT (id) DO UPDATE SET public = true;

       IF NOT EXISTS (SELECT 1 FROM pg_policies WHERE schemaname = 'storage' AND tablename = 'objects' AND policyname = 'allow_insert_for_everyone_' || bucket_id) THEN
         EXECUTE format('CREATE POLICY %I ON storage.objects FOR INSERT WITH CHECK (bucket_id = %L)', 'allow_insert_for_everyone_' || bucket_id, bucket_id);
       END IF;
       IF NOT EXISTS (SELECT 1 FROM pg_policies WHERE schemaname = 'storage' AND tablename = 'objects' AND policyname = 'allow_select_for_everyone_' || bucket_id) THEN
         EXECUTE format('CREATE POLICY %I ON storage.objects FOR SELECT USING (bucket_id = %L)', 'allow_select_for_everyone_' || bucket_id, bucket_id);
       END IF;
     END LOOP;

     FOREACH bucket_id IN ARRAY ARRAY[
       '23_avatar-images', '23_support-images', '23_support-guest-images', '23_product-personalization-images'
     ]
     LOOP
       IF NOT EXISTS (SELECT 1 FROM pg_policies WHERE schemaname = 'storage' AND tablename = 'objects' AND policyname = 'allow_update_for_everyone_' || bucket_id) THEN
         EXECUTE format('CREATE POLICY %I ON storage.objects FOR UPDATE USING (bucket_id = %L)', 'allow_update_for_everyone_' || bucket_id, bucket_id);
       END IF;
     END LOOP;
   END $$;
   ```

   Old `23_public-images`/`23_avatar-images` blocks stay in the doc under their own heading,
   marked retired — Nikita's files still live there until his manual rename (§1).

2. Update every `bucket:` argument and hardcoded bucket-name string listed in §2's table,
   including the 2 buckets added by §1's resolved questions (`23_ai-product-images`,
   `23_product-personalization-images`).
3. `pnpm tsc --noEmit -p .`, `pnpm vitest run app/api/backup`, `pnpm eslint` on every touched
   file — all clean before Stage 1 is done.

**Stage 2 — folders, filenames, backup/relink:**

1. Build `slugifyFileNameForBucket` and `slugifyEmail`.
2. Flip `createProductFn.tsx`'s pipeline order (Stripe product before image upload); update
   `uploadProductImages` and `FormatImagesForm.tsx` for `email/productId/slug-index.ext`.
3. Update `UpdateAvatarModal.tsx` to `slugifyEmail`. Update `uploadDesignFn.ts` to
   `slugifyEmail(email)/productId`, filename = the buyer's own uploaded file name (real, not
   generic — a file-picker upload, not a paste) run through `slugifyFileNameForBucket`.
4. Add the timestamp filename + signed-in/anonymous bucket-and-folder branch to
   `uploadImagesAndSendMessage.ts` and `MessagesFooter.tsx`.
5. Add a new **GUEST SUPPORT IMAGES CLEANUP** section to `dev_readme-supbase-sql.md`, directly
   under **ANONYMOUS TICKETS CLEANUP**, same test-query-first + idempotent `cron.unschedule` +
   `cron.schedule` shape (🚨 manual step, SQL in the commit):

   ```sql
   -- =================================== 🧹 GUEST SUPPORT IMAGES CLEANUP ===================================

   -- Test first, by hand: which objects in 23_support-guest-images would be deleted right now?
   SELECT storage.objects.name, storage.objects.created_at
   FROM storage.objects
   WHERE storage.objects.bucket_id = '23_support-guest-images'
     AND NOT EXISTS (
       SELECT 1 FROM public."23_messages" message
       WHERE message.images IS NOT NULL
         AND storage.objects.name = ANY (
           SELECT regexp_replace(image_url, '^.*/23_support-guest-images/', '')
           FROM unnest(message.images) AS image_url
         )
     );

   -- Schedule the weekly cleanup job (idempotent — safe to re-run)
   SELECT cron.unschedule('cleanup_guest_support_images')
   WHERE EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'cleanup_guest_support_images');

   SELECT cron.schedule('cleanup_guest_support_images', '0 3 * * 0', $$
     DELETE FROM storage.objects
     WHERE storage.objects.bucket_id = '23_support-guest-images'
       AND NOT EXISTS (
         SELECT 1 FROM public."23_messages" message
         WHERE message.images IS NOT NULL
           AND storage.objects.name = ANY (
             SELECT regexp_replace(image_url, '^.*/23_support-guest-images/', '')
             FROM unnest(message.images) AS image_url
           )
       );
   $$);
   ```

   Weekly, Sunday 03:00 UTC — deliberately not the same time as `cleanup_anonymous_tickets`
   (daily 03:00) or `weekly_ai_price_proposals` (Monday 03:00), so no two jobs fire together.

6. Extend relink to reconstruct the expected path from the row's current email + title, not only
   match the exported file's literal string — required, not optional, because Nikita's manual
   one-time rename (§1) means the old `img_url` strings will not match anything in Storage until
   relink can compute the new-scheme path itself.
7. Update `dev_readme-backup.md` and `dev_readme-supbase-sql.md` to match what shipped. Write in
   the corrected `auth.users`/`23_users.id` explanation from §0 word for word — `auth.users` is
   Supabase-managed and read-only, it is never exported or imported; only `public.23_users.csv`
   is, and its `id` column is rewritten from the old project-A uuid to a new project-B uuid
   before the upsert. This exact point caused real confusion while writing this plan — the
   shipped doc has to state it unambiguously, not just "accounts are prepared."
8. Write the 4 image prompts (support images, product images, avatar URL, backup import/export)
   into §5, ready to paste into an image generator — the prompts are the deliverable, not a
   generated page. Deep dark-green background, slightly rounded corners.
9. `pnpm vitest run`, `pnpm tsc --noEmit -p .`, `pnpm eslint` on every touched file — all clean
   before Stage 2 is done.
10. Once both stages are fully done, one repo-wide `pnpm lint` — not per task, not per stage, the
    single final gate after everything else in this plan is already committed.

## 5. Image prompts

Four prompts, one per workflow, written against what actually shipped — paste one into an image
generator as it stands, no editing needed. They share the same style line so the 4 read as one set.

**Shared style line** (already repeated at the end of each prompt below): "Deep dark-green
background, slightly rounded corners, readable in both a light and dark viewer theme, no decorative
filler — every box and arrow maps to a real step named in plan-22."

---

### 1. Support chat images (done)

> A technical flow diagram on a deep dark-green background, slightly rounded corners on every box.
>
> One box at the top, centred: "buyer pastes an image in the chat window", with a smaller line under
> it: "the file arrives as image.png — always". An arrow down, labelled "uploadImageFn", to a second
> centred box: "file name = the moment it arrived", showing `2026-07-29_at_22-19-54.png` in
> monospace, and under it: "Europe/Berlin — the zone a message created_at is stamped in".
>
> Below that box the flow splits into two lanes. The left arm is labelled "signed in", the right arm
> "not signed in".
>
> Left lane box: `23_support-images` as its title, then "folder = slugifyEmail(email)", then the
> monospace path `nicitaacomgmailcom/2026-07-29_at_22-19-54.png`, then "one folder per account, for
> good". Under it, a quiet outlined box: "nothing sweeps this bucket — the images belong to an
> account that stays".
>
> Right lane box: `23_support-guest-images` as its title, then "folder = deviceId, transport form",
> then the monospace path `Q5UUMP4MX0LbwF0Eekm3JIeIBwqWeDX0-/2026-07-29_at_22-20-11.png`, then "same
> visitor, same folder, every visit". A dashed arrow leaves it downward, labelled "Sunday 03:00
> UTC", into a dashed box: "weekly pg_cron sweep — cleanup_guest_support_images — deletes any object
> in this bucket that no 23_messages.images entry points at; an object younger than 1 hour is
> skipped".
>
> Use one colour for the signed-in lane and a different one for the guest lane, and keep those two
> colours consistent through their whole lane. Deep dark-green background, slightly rounded corners,
> readable in both a light and dark viewer theme, no decorative filler — every box and arrow maps to
> a real step named in plan-22.

---

### 2. Product images

> A 4-step vertical flow diagram. Deep dark-green background, slightly rounded corners, no icons —
> text only, monospace for every path.
>
> Title: "Product image → its path".
>
> Box 1: `title: "сливки 30%"`.
>
> Box 2: `slug: "slivki-30pct"` — small side note: "% → pct, cyrillic → latin".
>
> Box 3: `Stripe → productId: "prod_T1IRAxDEq5VtEmno"`.
>
> Box 4, one wide highlighted box, the result:
> `23_product-images/nicitaacomgmailcom/prod_T1IRAxDEq5VtEmno/slivki-30pct-1.jpg`. Under box 4,
> three short labels pointing up at the three parts of that path: "owner email, slugged" ·
> "productId — delete this folder, delete the product" · "title slug + position".
>
> Every label under 6 words. No decorative filler. Deep dark-green background, slightly rounded
> corners, readable in both a light and dark viewer theme, no decorative filler — every box and
> arrow maps to a real step named in plan-22.

---

### 3. Avatar URL

> A deliberately simple two-panel diagram on a deep dark-green background, slightly rounded corners.
> It is the simplest of the four and should say so: put the note "single file per account, always an
> intentional overwrite" above the top panel.
>
> Top panel, four boxes left to right joined by arrows: "user picks a new avatar image" → "folder =
> slugifyEmail(user.email)", showing `nicitaacomgmailcom` in monospace → "upload to
> `23_avatar-images` with `upsert: true`" → "the old avatar at that exact path is replaced".
>
> Bottom panel, titled "why the folder still holds after a restore", four smaller boxes left to
> right joined by arrows: "auth.users.id can change — a restore, a Google re-auth" → "23_users.id is
> rewritten to the new project's uuid" → "the email never changes" → "slugifyEmail(email) → the same
> folder as before".
>
> Deep dark-green background, slightly rounded corners, readable in both a light and dark viewer
> theme, no decorative filler — every box and arrow maps to a real step named in plan-22.

---

### 4. Backup import / export

> A two-column technical diagram on a deep dark-green background, slightly rounded corners on every
> box, joined by one labelled arrow between the columns.
>
> Left column, headed "Export — project A", two boxes that are independent flows, not a sequence:
> "GET /api/backup/rows — one CSV per table, packed into a .tar.gz in the browser" and "GET
> /api/backup/files — bucket/path/size list, no bytes; the browser downloads every file straight
> from Supabase and packs a second .tar.gz".
>
> The arrow between the columns is labelled "the admin uploads both exported .tar.gz files to
> project B".
>
> Right column, headed "Import — project B", four boxes top to bottom joined by arrows: "POST
> /api/backup/auth-users — reuse the matching project-B account by uuid or email, else create a new
> one" → "every FK column (owner_id, sender_id, user_id) is remapped to that project-B uuid, then
> the rows are upserted" → "POST /api/backup/files — a signed upload URL per path, bytes PUT
> straight to Supabase" → "POST /api/backup/relink-storage-urls — rewrites a restored URL once the
> file is confirmed present, and reports what is still unresolved".
>
> One wide callout box below both columns, connected to the relink box: "the expected path is
> computable from the row itself — 23_products.id plus slugifyEmail(owner email) gives
> `23_product-images/nicitaacomgmailcom/prod_T1IRAxDEq5VtEmno`; the files in that folder pair with
> the row's unresolved URLs in order, so relink no longer depends on the exported URL's literal
> string matching".
>
> Deep dark-green background, slightly rounded corners, readable in both a light and dark viewer
> theme, no decorative filler — every box and arrow maps to a real step named in plan-22.

---

## 6. Finishing the docs — embedding the 4 images

Each of §5's 4 prompts is generated one at a time (image-generator limit), then embedded by hand
into the doc that already documents that workflow. Not a Stage — nothing here touches app code.

| #   | Prompt                 | Status                                                                                                                             | Embeds into                                                                                 |
| --- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 1   | Support chat images    | **Done** — `public/docs/support/support-image-upload-workflow.png`, embedded in `dev_readme-supbase-sql.md` above the bucket table | `dev_readme-supbase-sql.md`, "SQL query for buckets + policies"                             |
| 2   | Product images         | **Done** — no generated image; embedded as an ASCII diagram instead (§5 prompt below kept as history, not used)                                                           | `app/functions/dev_readme-create-product.md`, §1 "How does it look", next to `Products.png` |
| 3   | Avatar URL             | Not generated yet                                                                                                                  | `dev_readme-supbase-sql.md`, "SQL query for buckets + policies", next to image 1            |
| 4   | Backup import / export | Not generated yet                                                                                                                  | `dev_readme-backup.md`, above "Export flow"                                                 |

For each of 2–4, once Nikita pastes the generated PNG:

1. Save it at `public/docs/<feature>/<name>.png` (kebab-case, matches the existing screenshots
   already in that folder).
2. Add a row to `public/docs/public-assets.md` under "Used images — subfolders".
3. Embed it with `![alt](public/docs/<feature>/<name>.png)` + the path line under it, into the doc
   named in the row above — same pattern image 1 already uses.

🚨 TODO

1. Generate images 3–4 from the §5 prompts — image-generator dashboard, not something run from this
   repo. Image 2 (Product images) is done as an ASCII diagram instead, no generated image needed.
