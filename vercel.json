{
  "version": 2,
  "builds": [
    { "src": "api/*.ts", "use": "@vercel/node-server" },
    { "src": "src/main.tsx", "use": "vercel-static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" },
    { "src": "/(.*)", "dest": "src/main.tsx" }
  ]
}