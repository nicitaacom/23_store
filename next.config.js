/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["ic.pics.livejournal.com", "images.unsplash.com", "lh3.googleusercontent.com"],
  },
}

module.exports = nextConfig
